请在这台 Mac 上安装 nLab API CLI 和 nlab-backend-bridge Skill。

要求：
1. 先下载并阅读 https://jacob-z.top/nlab-api/install.sh，确认它只会安装到当前用户的 ~/.local/bin 和 ~/.agents/skills；不要使用 sudo。
2. 确认系统是 macOS Apple Silicon，并且已有 curl、tar、git 和 codegraph。
3. 执行：curl -fsSL --proto '=https' --tlsv1.2 https://jacob-z.top/nlab-api/install.sh | sh
4. 验证 ~/.local/bin/nlab-api --version 可以运行，并确认 ~/.agents/skills/nlab-backend-bridge/SKILL.md 存在。
5. 如果 ~/.local/bin 不在 PATH，帮我写入当前 shell 的用户配置；不要改项目业务代码。
6. 告诉我安装版本、Skill 路径、验证结果，以及是否需要重启 AI 客户端。
